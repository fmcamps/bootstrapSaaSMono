---
interface Props {
  title?: string;
}

const { title } = Astro.props;
---

<div class="mb-4">
  {title && <h6 class="text-muted">{title}</h6>}
  <div class="p-3 rounded mb-2" style="border:1px solid var(--border-color);">
    <slot />
  </div>
</div>
